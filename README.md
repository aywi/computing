# computing

## Directory Structure

- `pixi/`: Scripts for Pixi
  - `setup_pixi.sh`: Set up Pixi as well as its environment variables and aliases
  - `setup_pixi_global.sh`: Set up Pixi global tools
- `git/`: Scripts for Git
  - `sync_git.sh`: Sync Git repositories from a list file
